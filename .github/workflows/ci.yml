name: Run CI checks

on: [pull_request]

jobs:
  test-affected:
    runs-on: ubuntu-latest
    name: Test Affected Apps
    steps:
      - uses: actions/checkout@v1
      - uses: bahmutov/npm-install@v1
      - run: npm run nx nx affected --target=test --base=origin/master --parallel
